#include "holberton.h"

/**
 * _strlen - check the code for Holberton School students.
 * @s: wopa
 * Return: Always 0.
 */

int _strlen(char *s)
{
	int a;

	for (a = 0; s[a] != '\0'; a++)
	{
	}
	return (a);
}
